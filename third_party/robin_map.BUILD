# robin_map BUILD file
# tsl::robin_map is a fast hash map used by nanobind

cc_library(
    name = "robin_map",
    hdrs = glob(["include/tsl/**/*.h"]),
    includes = ["include"],
    visibility = ["//visibility:public"],
)